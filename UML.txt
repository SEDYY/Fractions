@startuml
start

:Запуск формы FractionCalculatorForm;
:Инициализация интерфейса\n(текстовые поля, кнопки: Сложить, Вычесть, Умножить, Сравнить);

:Обработка события кнопки;
if (Нажата кнопка?) then (да)
    :Считывание данных из текстовых полей\n(целая и дробная части для двух чисел);
    
    :Проверка корректности ввода;
    if (Данные корректны?) then (да)
        if (Кнопка = Сложить или Вычесть или Умножить?) then (да)
            :Выполнение арифметической операции;
            :Вычисление результата с учётом знака и дробной части;
            
            :Вывод результата в текстовое поле;
        else (нет)
            if (Кнопка = Сравнить?) then (да)
                :Сравнение двух дробных чисел;
                :Определение больше/меньше/равно;
                
                :Вывод результата в текстовое поле\n(например, "Число 1 > Число 2");
            else (нет)
                :Пропуск действия;
            endif
        endif
    else (нет)
        :Вывод сообщения об ошибке\n(например, "Некорректный ввод");
    endif
else (нет)
    :Ожидание следующего события;
endif

stop
@enduml